//// LinePattern//function PatternLine(size,density,angle,color){	document.currentStyle.fillColor = null;	document.currentStyle.strokeColor = null;		var defaultLenght = 100;	var sprite = new Group();	var area = new Path.Rectangle(0,0,defaultLenght,size);		area.strokeColor = null;	area.fillColor = null;		sprite.appendChild(area);		area.fillColor = null;	area.strokeColor = null;		var l = new Path.Line(0,size/2,defaultLenght,size/2);		l.strokeColor = color;	l.strokeWidth = size*density;	l.fillColor = null;	sprite.appendChild(l);		//create the pattern color	var pattern = new Pattern();		pattern.definition = sprite;		var baseMatrix = new Matrix(1,0,0,-1,0,0);	var matrix = baseMatrix.rotate(angle);	var pc = new PatternColor(pattern, matrix);		sprite.remove();		return pc;	}//////function PatternSquare(size,density,angle,color){	document.currentStyle.fillColor = null;	document.currentStyle.strokeColor = null;		var sprite = new Group();	var area = new Path.Rectangle(0,0,size,size);		area.fillColor = null;	area.strokeColor = null;		sprite.appendChild(area);		var quad = new Path.Rectangle(0,0,size,size);				quad.fillColor = color;	quad.strokeColor = null;	quad.scale(density);	sprite.appendChild(quad);		//create the pattern color	var pattern = new Pattern();		pattern.definition = sprite;		var baseMatrix = new Matrix(1,0,0,-1,0,0);	var matrix = baseMatrix.rotate(angle);	var pc = new PatternColor(pattern, matrix);		sprite.remove();		return pc;}////circles//function PatternCircle(size,density,angle,color){		document.currentStyle.fillColor = null;	document.currentStyle.strokeColor = null;		var sprite = new Group();	var area = new Path.Rectangle(0,0,size,size);		area.fillColor = null;	area.strokeColor = null;		sprite.appendChild(area);		var circ = new Path.Circle(size/2,size/2,size/Math.sqrt(2));		circ.scale(density);	circ.fillColor = color;	circ.strokeColor = null;	sprite.appendChild(circ);		//create the pattern color	var pattern = new Pattern();		pattern.definition = sprite;		var baseMatrix = new Matrix(1,0,0,-1,0,0);	var matrix = baseMatrix.rotate(angle);	var pc = new PatternColor(pattern, matrix);		sprite.remove();		return pc;}